server {
    listen 80;
    
    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # THIS IS THE MAGIC LINE: 
        # It tells Nginx "If you can't find the file, just serve index.html"
        try_files $uri $uri/ /index.html; 
    }
}